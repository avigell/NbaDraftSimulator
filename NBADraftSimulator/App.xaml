<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NBADraftSimulator.App">
    <Application.Resources>
        <ResourceDictionary>

            <!-- ========== COLORI NBA (FISSI) ========== -->
            <Color x:Key="NBABlue">#1D428A</Color>
            <Color x:Key="NBARed">#C8102E</Color>
            <Color x:Key="NBAGold">#FFD700</Color>

            <!-- ========== COLORI PER TEMA CHIARO ========== -->
            <Color x:Key="LightBackground">#FFFFFF</Color>
            <Color x:Key="LightSecondaryBackground">#F5F5F5</Color>
            <Color x:Key="LightText">#000000</Color>
            <Color x:Key="LightSecondaryText">#666666</Color>
            <Color x:Key="LightBorder">#E0E0E0</Color>

            <!-- ========== COLORI PER TEMA SCURO ========== -->
            <Color x:Key="DarkBackground">#0A0A0A</Color>
            <Color x:Key="DarkSecondaryBackground">#1a1a1a</Color>
            <Color x:Key="DarkText">#FFFFFF</Color>
            <Color x:Key="DarkSecondaryText">#CCCCCC</Color>
            <Color x:Key="DarkBorder">#333333</Color>

            <!-- ========== STILI GLOBALI ========== -->

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource LightText}, Dark={StaticResource DarkText}}" />
            </Style>

            <Style TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}" />
                <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource LightBorder}, Dark={StaticResource DarkBorder}}" />
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="HasShadow" Value="True"/>
                <Setter Property="Padding" Value="10"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="BackgroundColor" Value="{StaticResource NBARed}"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>

            <Style x:Key="StatsLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource LightSecondaryText}, Dark={StaticResource DarkSecondaryText}}" />
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
            </Style>

            <Style x:Key="SectionTitle" TargetType="Label">
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource LightText}, Dark={StaticResource DarkText}}" />
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>